spring:
  profiles:
    active: @spring.profiles.active@
  datasource:
    url: jdbc:postgresql://localhost:5432/go-duck
    username: internal
    password: internal
    driver-class-name: org.postgresql.Driver

keycloak:
  auth-server-url: http://localhost:9080
  realm: master
  resource: internal
  credentials:
    secret: internal

multitenancy:
  enabled: true
